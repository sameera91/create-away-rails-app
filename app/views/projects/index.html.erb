<h1>All projects</h1>

<% @projects.each do |project| %>
  <% if current_user %>
    <%= link_to project.title, user_project_path(current_user.id, project.id) %><br>
  <% else %>
    <%= link_to project.title, project_path(project) %><br>
  <% end %>
  <%= project.short_description %><br>
<%end%>